# 演習 5-B 依存パッケージ
# 研修基盤に合わせてバージョンをピン留めしています。
# (本コースの基盤: langchain 1.3 系 / langgraph 1.2 系 / langchain-mcp-adapters 0.3 系)

langchain==1.3.7
langchain-openai==1.3.0
langgraph==1.2.4
langchain-mcp-adapters==0.3.0

# FastMCP サーバー (servers/knowledge_server.py) を動かすための公式 MCP SDK。
# バージョン指定は必須です。mcp 2.0.0 (2026-07-28 リリース) で FastMCP は MCPServer に改称され、
# `mcp.server.fastmcp` モジュールが削除されました。バージョンを指定しないと 2.x が入り、
# サーバー起動時に ModuleNotFoundError: No module named 'mcp.server.fastmcp' で失敗します。
# 下記は 1.x 系の最終版で、langchain-mcp-adapters 0.3.0 (mcp>=1.9.2 を要求) と互換です。
mcp==1.29.1

# .env から API キーを読み込むため
python-dotenv
